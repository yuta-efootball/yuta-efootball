# eFootball タレントデザイン シミュレーター v2.8.1

## 修正内容

### `liveLinkTargets` 列を廃止

選手CSVから `liveLinkTargets` 列を削除しました。

JavaScriptからも、この列に依存していた処理を削除しています。

- CSV読み込み・正規化
- `liveLinkTargets` の入力チェック
- 選手画面へのライブリンク対象表示
- ライブリンク対象を選手側ブースター対象として扱う処理
- A / B / C によるライブリンク能力加算
- ライブリンクのイベント処理

既存の `index.html` にライブアップデートUIが残っている場合も、`script.js` の起動時に自動的に非表示化します。

### 維持している内容

v2.8で設定した最終能力値カードの上下余白 `0.15em` は変更していません。

## 今回アップロードするファイル

- `script.js`
- `players.csv`
- `README.md`

GitHubでは、この3ファイルを現在のものと置き換えてください。
